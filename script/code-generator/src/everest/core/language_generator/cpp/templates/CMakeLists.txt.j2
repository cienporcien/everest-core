{% from "helper_macros.j2" import insert_block, print_template_info %}
{{ print_template_info('4', 'marked regions will be kept', '#') }}

# module setup:
#   - ${MODULE_NAME}: module name
ev_setup_cpp_module()

{{ insert_block(blocks.add_general) }}

target_sources(${MODULE_NAME}
    PRIVATE
        {% for impl_file in impl_files %}
        "{{ impl_file }}"
        {% endfor %}
)

{{ insert_block(blocks.add_other) }}
